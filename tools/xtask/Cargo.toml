[package]
name = "xtask"
description = "Repository orchestration: golden-frame generation for the state ABI, and the release manifest of pinned values"
version = "0.1.0"
edition.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
indicate-instrument-glyphs = { workspace = true }
indicate-instrument-panels = { workspace = true }
indicate-instrument-registry = { workspace = true }
indicate-instrument-scene = { workspace = true }
indicate-instrument-state = { workspace = true }
# The fixture screen and its pinned digest have one definition, in the
# bench that checks them; the manifest records that definition rather
# than a second copy of it.
instrument-bench = { path = "../instrument-bench" }
thiserror = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
