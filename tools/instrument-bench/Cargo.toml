[package]
name = "instrument-bench"
description = "Standalone instrument bench shell: registry × canonical states → admission, scene digest, raster frames — no host, no protocol (ADR-0029)"
version = "0.1.0"
edition.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
indicate-instrument-conformance = { workspace = true }
indicate-instrument-panels = { workspace = true }
indicate-instrument-raster = { workspace = true }
indicate-instrument-registry = { workspace = true }
indicate-instrument-scene = { workspace = true }
indicate-instrument-state = { workspace = true }
thiserror = { workspace = true }
