[package]
name = "indicate-instrument-conformance"
description = "Panel admission harness: layer, background-contract, budget, glyph, and honest-status conformance over the registry corpus (ADR-0029, ADR-0033)"
version = "0.1.0"
edition.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
indicate-alerts = { workspace = true }
indicate-instrument-glyphs = { workspace = true }
indicate-instrument-registry = { workspace = true }
indicate-instrument-scene = { workspace = true }
indicate-instrument-state = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
indicate-instrument-panels = { workspace = true }
